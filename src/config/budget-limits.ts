export const BUDGET = {
  // Max amount to spend on a single scan/agent (in USDT)
  MAX_TEST_BUDGET_PER_SCAN: 0.02,
  
  // Max total amount to spend in a single day (in USDT)
  MAX_TEST_BUDGET_TOTAL_PER_DAY: 0.5,
};
